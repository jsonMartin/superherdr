## 1. Binary target and directories

- [ ] 1.1 In `Cargo.toml`, add `[[bin]] name = "herdr"` with `path = "src/main.rs"`, set `version` to Herdr's base `0.9.0`, change `include` to `skills/herdr/SKILL.md`, then run `cargo update -p superherdr --offline`. Verify: `cargo build --locked` produces `target/debug/herdr` and no `target/debug/superherdr`.
- [ ] 1.2 In `src/build_info.rs`, add the Superherdr revision (`1`) and a four-part `superherdr_version()`; print `herdr <base>[-<channel suffix>] (superherdr <version>)` from `src/main.rs`; add optional `superherdr_version` to `ResponseResult::Pong` in `src/api/schema/response.rs`, set it in `src/api/server.rs`, carry it through `src/api/client.rs` into runtime status, add it to client and server status JSON in `src/cli/status.rs`, make the stale-server check prefer it when both sides report it, and regenerate `docs/next/api/herdr-api.schema.json` with `HERDR_UPDATE_API_SCHEMA=1`. Verify: `cargo run -- --version` prints `herdr 0.9.0 (superherdr 0.9.0.1)` and status unit tests cover same-base different-revision staleness.
- [ ] 1.3 In `src/config/io.rs`, make `app_dir_name()` return `herdr` and `herdr-dev`; in `src/config/model.rs`, set the default `worktrees.directory` to `~/.herdr/worktrees`; in `src/main.rs`, return `DEFAULT_CONFIG` comments to upstream wording except the `version_check` lines. Verify: config unit tests pass and `cargo run -- --default-config | head -2` names `~/.config/herdr/config.toml`.
- [ ] 1.4 Update `justfile` (`--bin herdr`, `target/release/herdr`), `.github/workflows/build-linux-release.yml` (binary path, `--version` check, smoke config directory `herdr`, socket path, archive entry `herdr`), `flake.nix` and `nix/package.nix` (`mainProgram`, apps program path, skill path). Verify: `just build` succeeds and the workflow parses with `python3 -c 'import yaml,sys; yaml.safe_load(open(sys.argv[1]))' .github/workflows/build-linux-release.yml`.

## 2. Command wording, remote discovery and skill

- [ ] 2.1 Revert the rename commit's `superherdr` command strings in `src/` to upstream text: `src/cli.rs`, `src/cli/*.rs` including the clap command name in `src/cli/spec.rs` and the schema banner in `src/cli/api.rs`, `src/main.rs` help and errors, `src/session.rs`, `src/server/*`, `src/client/*`, `src/remote.rs`, `src/remote/host_unix.rs`, `src/remote/saved.rs`, `src/platform/unix_common.rs`, `src/integration/registry.rs`, the diagnostic suffix in `src/config/io.rs`, test strings in `src/app/mod.rs`, and the usage line in `src/update.rs`. Keep the `--version` product name, the self-update guard and message, the `version_check` default, `HERDR_PLUGIN_COMPATIBILITY_VERSION` and the download refusal. Verify: `git grep -n superherdr -- src` lists only those retained lines and comments.
- [ ] 2.2 In `src/remote/attach.rs`, return the default remote binary, known-path discovery script, `command -v` probe and mise shim check to `herdr` (upstream text), return the `remote_reattach_program` fallback, `install_suffix` (`.local/bin/herdr`), `warn_if_remote_bin_not_on_path` and `remote_shell_resolves_managed_install` to `herdr`, and reword the refusal to "Superherdr does not download release binaries for remote hosts; set HERDR_REMOTE_BINARY to a Superherdr build for the remote platform or install herdr there". Verify: `just test-one known_remote_binary_candidate_script` and the install-source description tests pass.
- [ ] 2.3 Restore `skills/herdr/SKILL.md` from upstream with `git show b99002ac:skills/herdr/SKILL.md`, delete `skills/superherdr/`, remove `skills/herdr/` from `.upstream-sync/exclude`, and point `include_str!` in `src/main.rs` at the restored file. Verify: `cargo run -- --skill | head -3` prints `name: herdr` and `just upstream-exclusions-check` passes.

## 3. Tests and scripts

- [ ] 3.1 Across `tests/` (`tests/cli/*.rs`, `tests/*.rs`, `tests/support/mod.rs`), replace `CARGO_BIN_EXE_superherdr` with `CARGO_BIN_EXE_herdr`, `superherdr-dev` with `herdr-dev`, `target/debug/superherdr` with `target/debug/herdr`, and `superherdr` command expectations with `herdr`. Verify: `just test` passes.
- [ ] 3.2 Replace `tests/superherdr_rename.rs` with `tests/drop_in_herdr.rs` asserting: `--version` matches `herdr <base>[-<suffix>] (superherdr <base>.<revision>)`; `status client --json` has a three-part `version` and a four-part `superherdr_version`; `--help` contains `Usage: herdr` and the `herdr-dev` or `herdr` directory name; `completion zsh` contains `#compdef herdr`; `--skill` contains `name: herdr` and not `superherdr`; `update` fails with "self-update is disabled for Superherdr". Verify: `just test-one drop_in_herdr` passes.
- [ ] 3.3 Add a persistence test that a `session.json` written by Superherdr loads after `lifetime_id` is removed, and a configuration test that a file containing only Herdr keys loads with no diagnostics. Verify: `just test-one lifetime_id` and `just test-one herdr_keys` pass.
- [ ] 3.4 Update `scripts/smoke_live_handoff_sessions.sh`, `scripts/seed_navigator_demo.sh`, `scripts/release_perf_case.sh` and `scripts/windows_check.ps1` to `herdr` and `herdr-dev` paths. Verify: `sh -n` passes on each shell script.

## 4. Shell installer

- [ ] 4.1 Rewrite `distribution/install.sh`: `DEFAULT_VERSION="0.9.0.1"` and accept four-part versions; install a single `herdr`; `check_destinations` applies the ownership rules (owned is a regular `herdr` whose `--version` contains `(superherdr `, or the 0.1.0 pair; an owned `herdr` beside a leftover regular `superherdr` resumes an interrupted 0.1.0 upgrade; `-x` is checked before running a candidate; every `PATH` entry other than the install directory is scanned for executable `herdr` and `superherdr`; everything else is refused with the path before any download); stage and rename; then delete the owned 0.1.0 `superherdr`; final text says `run 'herdr'`. Verify: `sh -n distribution/install.sh` and `python3 -m unittest scripts.test_unix_installer` pass.
- [ ] 4.2 Extend `scripts/test_unix_installer.py` with executable previous-install fixtures (the 0.1.0 pair, an owned `herdr` printing `herdr 0.9.0 (superherdr 0.9.0.1)`, an upstream `herdr` printing `herdr 0.9.0`) and cases: fresh install creates only `herdr`; upgrade over an owned `herdr`; upgrade over the 0.1.0 pair removes `superherdr`; refuse upstream `herdr`; refuse a non-executable `herdr`; refuse a foreign symlink; refuse `PATH` conflicts including a `herdr` shadowed later on `PATH`; resume an interrupted 0.1.0 upgrade (owned `herdr` plus leftover `superherdr`); archive missing `herdr`; checksum, extraction and rename failures preserve both previous layouts. Verify: the suite passes and every refusal case asserts that no curl call happened.

## 5. Documentation and release notes

- [ ] 5.1 Update `README.md` (install, use and compatibility sections: the `herdr` command, shared Herdr directories, 0.1.0 directories no longer read, how to replace upstream Herdr, remote behavior) and `docs/next/homebrew.md`; add `docs/next/superherdr-0.9.0.1.md` release notes; keep `docs/next/superherdr-0.1.0.md` as history. Verify: `git grep -n 'superherdr ' -- README.md docs/next/homebrew.md docs/next/superherdr-0.9.0.1.md` shows no command examples.
- [ ] 5.2 Update `AGENTS.md` and `CONTRIBUTING.md` (`herdr` command, `herdr-dev`, `~/.config/herdr/agent-detection`, `skills/herdr/SKILL.md`) and `openspec/README.md` (evidence pointer `tests/drop_in_herdr.rs`, row title for shared Herdr state). Verify: `git grep -n superherdr -- AGENTS.md CONTRIBUTING.md` shows only package, tag, tap and repository names.
- [ ] 5.3 Add an Unreleased entry to `CHANGELOG.md` covering the single `herdr` command, Herdr directories, 0.1.0 directories no longer read, installer upgrade behavior, the worktree default and remote discovery. Verify: the entry is present under an Unreleased heading.

## 6. Homebrew and AUR working copies

- [ ] 6.1 In `homebrew-tap/Formula/superherdr.rb`, install `bin/"herdr"` only, drop the `superherdr` symlink, keep `conflicts_with "herdr"`, and make the test assert `herdr --version` prints `herdr 0.9.0 (superherdr 0.9.0.1)`; set `version "0.9.0.1"`; update `homebrew-tap/README.md`. Version, URL, sha256 and bottle are filled when the 0.1.1 assets exist. Verify: `brew style` and `brew audit --strict jsonmartin/tap/superherdr` pass once the assets exist.
- [ ] 6.2 In `aur/superherdr-bin/PKGBUILD`, install `/usr/bin/herdr` only with no symlink, set `pkgver=0.9.0.1`, keep `provides` and `conflicts`, and regenerate `.SRCINFO` with `makepkg --printsrcinfo`. Verify: `namcap PKGBUILD` reports no errors, and `makepkg -si` succeeds on an Arch host once the assets exist.

## 7. Finish

- [ ] 7.1 Run `just check` and `just upstream-exclusions-check`. Verify: both pass and `git grep -c superherdr -- src` is down to the retained divergences.
- [ ] 7.2 Archive with `bunx --bun @fission-ai/openspec@1.13.0 archive drop-in-herdr`, then reword the `superherdr-compatibility` Purpose sentence to describe shared Herdr state as a documentation backfill, and run `just spec-check`. Verify: the baseline validates and the archived change is committed in the implementation PR.
- [ ] 7.3 The 0.9.0.1 release (tag, archives containing `herdr`, `install.sh`, `SHA256SUMS`, formula and AUR publication) follows through the maintainer's release process and is not part of this change.
