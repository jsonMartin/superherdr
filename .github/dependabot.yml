version: 2
updates:
  # Cargo dependencies follow upstream Herdr through sync pull requests; separate
  # bumps would conflict in Cargo.lock on every sync.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 1
    groups:
      github-actions:
        patterns:
          - "*"
